import React from "react"
import { Link } from "gatsby"
import Layout from "../components/Layout"

const Blog = () => (
  <Layout>
    hello from blog page!<Link to="/">back to home</Link>
  </Layout>
)

export default Blog
